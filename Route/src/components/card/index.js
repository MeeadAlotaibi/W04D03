import React from "react";
import { useParams } from "react-router";

const Card = () => {
  // console.log(usePrams);
  return (
    <div>
      <h1>Card</h1>
    </div>
  );
};

export default Card;
